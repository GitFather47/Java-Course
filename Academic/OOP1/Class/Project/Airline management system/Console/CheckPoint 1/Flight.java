import java.util.Scanner;class Flight extends Passenger{	    private String flightNo;        private String pilotName;        private String arrivalTime;        private String departureTime;        private String from;        private String to;        private String Class;        private String[][] seat;public Flight() {            seat = new String[8][4];            for (int i = 0; i < 8; i++) {                for (int j = 0; j < 4; j++) {                	if (seat[i][j]==null){                		seat[i][j] = "Empty";                	}                }            }        }public void install{            Scanner input = new Scanner(System.in);            System.out.print("Enter flight no: ");            flightNo = input.nextLine();            System.out.print("\nEnter pilot's name: ");            pilotName = input.nextLine();            System.out.print("\nArrival time: ");            arrivalTime = input.nextLine();            System.out.print("\nDeparture: ");            departatureTime = input.nextLine();            System.out.print("\nFlying From : \t\t\t");            from = input.nextLine();            System.out.print("\nFlying To: \t\t\t");            to = input.nextLine();            System.out.print("\nClass: \t\t\t");            Class = input.nextLine();}public void avail(){	for (int n = 0; n < p; n++) {                System.out.println("Flight no: \t" + flight[n].flightn + "\nPilot name: \t" + flight[n].pilot + "\t\tArrival time: \t"                        + flight[n].arrival + "\tDeparture Time: " + flight[n].depart + "\nFrom: \t\t" + flight[n].from  + "\t\tTo: \t\t\t" + flight[n].to + "\n");            }public void check(){	}}